---
chapter_id: '1.10.4'
title: '打印自定义文档'
official_title: 'Print custom documents'
official_url: 'https://developer.android.com/training/printing/custom-docs'
topic_url: 'https://developer.android.com/training/printing'
status: 'done'
author: 'minimax - OpenClaw'
volume_priority: 1
volume_grade: 'A'
chapter_importance: 3
plot_summary:
  time: '春天'
  location: '营地'
  scene: '黛琳讲自定义文档打印'
  season: '春季'
  environment: '春意盎然'
  topic: '自定义打印'
  discussion: 'PrintDocumentAdapter完整实现'
  problem_solved: '掌握自定义打印'
  difficulty: '中等'
  next_topic: '（第1卷存储部分完）'
---

# 1.10.4 打印自定义文档

"复杂打印需要完整实现PrintDocumentAdapter，"黛琳讲解道。

```kotlin
class MyPrintAdapter(val context: Context) : PrintDocumentAdapter() {
    
    private var pageCount = 1
    
    override fun onLayout(oldAttributes: PrintAttributes?, 
                         newAttributes: PrintAttributes?,
                         cancellationSignal: CancellationSignal?,
                         callback: LayoutResultCallback?, extras: Bundle?) {
        
        if (cancellationSignal?.isCanceled == true) {
            callback.onLayoutCancelled()
            return
        }
        
        pageCount = calculatePageCount(newAttributes)
        
        val info = PrintDocumentInfo.Builder("document.pdf")
            .setContentType(PrintDocumentInfo.CONTENT_TYPE_DOCUMENT)
            .setPageCount(pageCount)
            .build()
        
        callback.onLayoutFinished(info, true)
    }
    
    override fun onWrite(pages: Array<out Range<out Int>>?,
                       destination: FileDescriptor?,
                       cancellationSignal: CancellationSignal?,
                       callback: WriteResultCallback?) {
        
        val document = PdfDocument()
        
        for (i in 0 until pageCount) {
            val pageInfo = PdfDocument.PageInfo.Builder(595, 842, i).create()
            val page = document.startPage(pageInfo)
            
            // 绘制内容
            val canvas = page.canvas
            val paint = Paint()
            paint.textSize = 24f
            canvas.drawText("Page ${i + 1}", 100f, 100f, paint)
            
            document.finishPage(page)
        }
        
        document.writeTo(FileOutputStream(destination))
        document.close()
        
        callback.onWriteFinished(arrayOf(Range(0, pageCount - 1)))
    }
    
    private fun calculatePageCount(attrs: PrintAttributes?): Int {
        return 1 // 根据内容计算
    }
}
```

---

## 洛芙的小小日记本

> 自定义打印最复杂但最灵活！学会了就什么都能打印啦💪🌸
