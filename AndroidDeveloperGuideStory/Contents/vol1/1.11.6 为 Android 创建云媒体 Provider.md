---
chapter_id: '1.11.6'
title: '为 Android 创建云媒体 Provider'
official_title: 'Create a cloud media provider for Android'
official_url: 'https://developer.android.com/training/building-cloud-provider'
topic_url: 'https://developer.android.com/guide/topics/providers'
status: 'done'
author: 'minimax - OpenClaw'
volume_priority: 1
volume_grade: 'A'
chapter_importance: 3
plot_summary:
  time: '春天'
  location: '营地'
  scene: '黛琳讲云媒体Provider'
  season: '春季'
  environment: '春云悠悠'
  topic: '云媒体Provider'
  discussion: '实现云端存储的MediaProvider'
  problem_solved: '掌握云媒体Provider'
  difficulty: '困难'
  next_topic: '（第1卷完）'
---

# 1.11.6 创建云媒体Provider

"云存储可以用MediaProvider接口暴露给系统，"黛琳介绍道。

## 实现CloudMediaProvider

```kotlin
class MyCloudMediaProvider : CloudMediaProvider() {
    
    override fun onCreate(): Boolean = true
    
    override fun queryMedia(context: Context, cloudStorageOptions: CloudStorageOptions): List<MediaStore> {
        // 从云端获取媒体列表
    }
    
    override fun searchMedia(context: Context, cloudStorageOptions: CloudStorageOptions, 
                           query: String): List<MediaStore> {
        // 搜索媒体
    }
    
    override fun getThumbnail(context: Context, cloudStorageOptions: CloudStorageOptions,
                            mediaStore: MediaStore, size: Size, 
                            cancellationSignal: CancellationSignal?): Bitmap {
        // 获取缩略图
    }
    
    override fun openFile(context: Context, cloudStorageOptions: CloudStorageOptions,
                        mediaStore: MediaStore, mode: String): ParcelFileDescriptor {
        // 打开文件
    }
    
    override fun deleteMedia(context: Context, cloudStorageOptions: CloudStorageOptions,
                           mediaStore: MediaStore): Boolean {
        // 删除媒体
    }
}
```

## Manifest配置

```xml
<provider
    android:name=".MyCloudMediaProvider"
    android:authorities="com.example.cloudmedia"
    android:exported="true"
    android:permission="android.permission.BIND_CLOUD_MEDIA_SERVICE">
    <meta-data
        android:name="android.content.cloudmedia.SUPPORTED_URI_PERMISSIONS"
        android:resource="@array/uri_permissions" />
</provider>
```

## 总结

云媒体Provider让云端文件能被系统媒体库识别。

---

## 洛芙的小小日记本

> 云媒体Provider好高级！学会了就能让云文件和本地文件一样被系统管理。春天🌸太美啦！
