---
chapter_id: '1.3.1.1'
title: '媒体文件基础'
official_title: 'Media files basics'
official_url: 'https://developer.android.com/training/data-storage/media'
topic_url: 'https://developer.android.com/training/data-storage'
status: 'done'
author: 'minimax - OpenClaw'
volume_priority: 1
volume_grade: 'A'
chapter_importance: 3
plot_summary:
  time: '春天'
  location: '营地'
  scene: '黛琳讲媒体文件基础'
  season: '春季'
  environment: '春光明媚'
  topic: '媒体文件'
  discussion: 'MediaStore API、媒体扫描'
  problem_solved: '掌握媒体文件基础'
  difficulty: '简单'
  next_topic: '（续）'
---

# 1.3.1.1 媒体文件基础

"媒体文件用MediaStore管理，"黛琳介绍道。

## 查询图片

```kotlin
fun queryImages(context: Context): List<Uri> {
    val images = mutableListOf<Uri>()
    
    val projection = arrayOf(
        MediaStore.Images.Media._ID,
        MediaStore.Images.Media.DISPLAY_NAME,
        MediaStore.Images.Media.DATE_ADDED
    )
    
    context.contentResolver.query(
        MediaStore.Images.Media.EXTERNAL_CONTENT_URI,
        projection, null, null,
        "${MediaStore.Images.Media.DATE_ADDED} DESC"
    )?.use { cursor ->
        val idColumn = cursor.getColumnIndexOrThrow(MediaStore.Images.Media._ID)
        while (cursor.moveToNext()) {
            val id = cursor.getLong(idColumn)
            val uri = ContentUris.withAppendedId(
                MediaStore.Images.Media.EXTERNAL_CONTENT_URI, id
            )
            images.add(uri)
        }
    }
    
    return images
}
```

---

## 洛芙的小小日记本

> MediaStore太有用了！可以管理所有媒体文件呀🌸
