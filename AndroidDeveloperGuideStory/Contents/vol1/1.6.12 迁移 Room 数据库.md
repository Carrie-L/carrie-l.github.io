---
chapter_id: '1.6.12'
title: '迁移 Room 数据库'
official_title: 'Migrate Room database'
official_url: 'https://developer.android.com/training/data-storage/room/migrating'
topic_url: 'https://developer.android.com/training/data-storage'
status: 'done'
author: 'minimax - OpenClaw'
volume_priority: 1
volume_grade: 'A'
chapter_importance: 4
plot_summary:
  time: '春天'
  location: '营地'
  scene: '黛琳讲数据库迁移'
  season: '春季'
  environment: '春光明媚'
  topic: '数据库迁移'
  discussion: 'Migration、fallbackToDestructiveMigration'
  problem_solved: '掌握迁移'
  difficulty: '困难'
  next_topic: '测试调试'
---

# 1.6.12 迁移Room数据库

"数据库版本升级需要迁移，"黛琳讲解道。

## 简单迁移

```kotlin
val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(database: SupportSQLiteDatabase) {
        database.execSQL("ALTER TABLE users ADD COLUMN age INTEGER NOT NULL DEFAULT 0")
    }
}

val MIGRATION_2_3 = object : Migration(2, 3) {
    override fun migrate(database: SupportSQLiteDatabase) {
        database.execSQL("CREATE TABLE books (id INTEGER PRIMARY KEY, title TEXT)")
    }
}

val database = Room.databaseBuilder(
    context, AppDatabase::class.java, "database"
)
    .addMigrations(MIGRATION_1_2, MIGRATION_2_3)
    .build()
```

## 破坏性迁移

```kotlin
// 开发阶段可以用这个，清空数据
val database = Room.databaseBuilder(
    context, AppDatabase::class.java, "database"
)
    .fallbackToDestructiveMigration()
    .build()
```

---

## 洛芙的小好记本

> 迁移要注意！最好用正式迁移，开发时用fallback🌸
