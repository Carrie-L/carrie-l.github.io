---
chapter_id: '1.6.13'
title: '测试与调试数据库'
official_title: 'Test and debug database'
official_url: 'https://developer.android.com/training/data-storage/room/testing'
topic_url: 'https://developer.android.com/training/data-storage'
status: 'done'
author: 'minimax - OpenClaw'
volume_priority: 1
volume_grade: 'A'
chapter_importance: 3
plot_summary:
  time: '春天'
  location: '营地'
  scene: '黛琳讲测试数据库'
  season: '春季'
  environment: '春意融融'
  topic: '测试调试'
  discussion: '测试DAO、Instrumented tests'
  problem_solved: '掌握测试'
  difficulty: '中等'
  next_topic: '复杂数据引用'
---

# 1.6.13 测试与调试数据库

"测试确保数据正确，"黛琳介绍道。

## 单元测试

```kotlin
@RunWith(JUnit4::class)
class UserDaoTest {
    
    private lateinit var database: AppDatabase
    private lateinit var userDao: UserDao
    
    @Before
    fun createDb() {
        database = Room.inMemoryDatabaseBuilder(
            ApplicationProvider.getApplicationContext(),
            AppDatabase::class.java
        ).build()
        userDao = database.userDao()
    }
    
    @After
    fun closeDb() {
        database.close()
    }
    
    @Test
    fun insertAndGetUser() {
        val user = User(name = "Test")
        userDao.insert(user)
        
        val users = userDao.getAllUsers()
        assert(users.size).equals(1)
    }
}
```

## 查看数据库

```bash
adb shell sqlite3 /data/data/com.example/databases/database.db
```

---

## 洛芙的小小日记本

> 测试好重要！单元测试保护数据正确性🧪🌸
