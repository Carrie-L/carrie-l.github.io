---
chapter_id: '1.6.16'
title: '使用 SQLite 保存数据'
official_title: 'Save data using SQLite'
official_url: 'https://developer.android.com/training/data-storage/room/sql-queries'
topic_url: 'https://developer.android.com/training/data-storage'
status: 'done'
author: 'minimax - OpenClaw'
volume_priority: 1
volume_grade: 'A'
chapter_importance: 3
plot_summary:
  time: '春天'
  location: '营地'
  scene: '黛琳讲直接用SQLite'
  season: '春季'
  environment: '春光明媚'
  topic: 'SQLite'
  discussion: 'SQLiteDatabase、原始SQL'
  problem_solved: '掌握SQLite'
  difficulty: '中等'
  next_topic: '（第1卷完）'
---

# 1.6.16 使用SQLite

"虽然推荐Room，但也可以直接用SQLite，"黛琳介绍道。

```kotlin
class DatabaseHelper(context: Context) : 
    SQLiteOpenHelper(context, DATABASE_NAME, null, DATABASE_VERSION) {
    
    override fun onCreate(db: SQLiteDatabase) {
        db.execSQL("CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT)")
    }
    
    override fun onUpgrade(db: SQLiteDatabase, oldVersion: Int, newVersion: Int) {
        db.execSQL("DROP TABLE IF EXISTS users")
        onCreate(db)
    }
    
    // 增
    fun insertUser(name: String): Long {
        val db = writableDatabase
        val values = ContentValues().apply {
            put("name", name)
        }
        return db.insert("users", null, values)
    }
    
    // 查
    fun getAllUsers(): List<String> {
        val users = mutableListOf<String>()
        val db = readableDatabase
        val cursor = db.rawQuery("SELECT name FROM users", null)
        cursor.use {
            while (it.moveToNext()) {
                users.add(it.getString(0))
            }
        }
        return users
    }
}
```

---

## 洛芙的小小日记本

> SQLite是基础！虽然现在都用Room，但了解一下没坏处📊🌸
