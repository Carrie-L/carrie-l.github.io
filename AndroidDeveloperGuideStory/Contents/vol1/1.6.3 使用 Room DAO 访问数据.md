---
chapter_id: '1.6.3'
title: '使用 Room DAO 访问数据'
official_title: 'Access data with Room DAOs'
official_url: 'https://developer.android.com/training/data-storage/room/dao'
topic_url: 'https://developer.android.com/training/data-storage'
status: 'done'
author: 'minimax - OpenClaw'
volume_priority: 1
volume_grade: 'A'
chapter_importance: 5
plot_summary:
  time: '春天'
  location: '营地'
  scene: '黛琳讲Room DAO'
  season: '春季'
  environment: '春花绽放'
  topic: 'Room DAO'
  discussion: '@Dao、@Query、@Insert、@Update、@Delete'
  problem_solved: '掌握DAO定义和使用'
  difficulty: '简单'
  next_topic: 'Room关系'
---

# 1.6.3 Room DAO访问数据

"DAO定义数据的增删改查操作，"黛琳介绍道。

## 基本DAO

```kotlin
@Dao
interface UserDao {
    
    @Query("SELECT * FROM users")
    fun getAllUsers(): List<User>
    
    @Query("SELECT * FROM users WHERE id = :id")
    fun getUserById(id: Long): User?
    
    @Query("SELECT * FROM users WHERE name LIKE :name")
    fun searchUsers(name: String): List<User>
    
    @Insert
    fun insert(user: User): Long
    
    @Insert
    fun insertAll(vararg users: User)
    
    @Update
    fun update(user: User)
    
    @Delete
    fun delete(user: User)
    
    @Query("DELETE FROM users")
    fun deleteAll()
}
```

## 异步操作

```kotlin
@Dao
interface AsyncUserDao {
    
    @Query("SELECT * FROM users")
    suspend fun getAllUsers(): List<User>
    
    @Insert
    suspend fun insert(user: User): Long
    
    @Transaction
    suspend fun insertAndGet(user: User): User {
        val id = insert(user)
        return getUserById(id)!!
    }
}
```

---

## 洛芙的小小日记本

> DAO好强大！用注解就能定义各种查询。协程异步超棒🌸
