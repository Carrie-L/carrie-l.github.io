---
chapter_id: '1.6.9'
title: '编写异步 DAO 查询'
official_title: 'Write asynchronous DAO queries'
official_url: 'https://developer.android.com/training/data-storage/room/async-queries'
topic_url: 'https://developer.android.com/training/data-storage'
status: 'done'
author: 'minimax - OpenClaw'
volume_priority: 1
volume_grade: 'A'
chapter_importance: 4
plot_summary:
  time: '春天'
  location: '营地'
  scene: '黛琳讲异步查询'
  season: '春季'
  environment: '春风拂面'
  topic: '异步查询'
  discussion: 'Kotlin协程、LiveData、Flow'
  problem_solved: '掌握异步查询'
  difficulty: '中等'
  next_topic: '数据库视图'
---

# 1.6.9 异步DAO查询

"数据库操作必须在后台线程，"黛琳强调道。

## Kotlin协程

```kotlin
@Dao
interface UserDao {
    
    @Query("SELECT * FROM users")
    suspend fun getAllUsers(): List<User>
    
    @Query("SELECT * FROM users WHERE id = :id")
    suspend fun getUserById(id: Long): User?
    
    @Insert
    suspend fun insert(user: User): Long
    
    @Update
    suspend fun update(user: User)
    
    @Delete
    suspend fun delete(user: User)
}
```

## Flow实时更新

```kotlin
@Dao
interface UserDao {
    
    @Query("SELECT * FROM users")
    fun getAllUsersFlow(): Flow<List<User>>
    
    @Query("SELECT * FROM users WHERE id = :id")
    fun getUserByIdFlow(id: Long): Flow<User?>
}
```

---

## 洛芙的小小日记本

> Flow好棒！数据变化自动更新🌸
