---
chapter_id: '1.9.5'
title: '获取文件信息'
official_title: 'Get file information'
official_url: 'https://developer.android.com/training/sharing/fileinfo'
topic_url: 'https://developer.android.com/training/sharing'
status: 'done'
author: 'minimax - OpenClaw'
volume_priority: 1
volume_grade: 'A'
chapter_importance: 2
plot_summary:
  time: '春天'
  location: '营地'
  scene: '黛琳讲获取文件信息'
  season: '春季'
  environment: '春意融融'
  topic: '文件信息'
  discussion: '从URI获取文件名、大小、MIME类型'
  problem_solved: '掌握文件信息获取'
  difficulty: '简单'
  next_topic: '（第1卷完）'
---

# 1.9.5 获取文件信息

"从content:// URI可以获取文件信息，"黛琳介绍道。

## 获取文件名

```kotlin
fun getFileName(context: Context, uri: Uri): String? {
    var name: String? = null
    
    if (uri.scheme == "content") {
        context.contentResolver.query(uri, null, null, null, null)?.use { cursor ->
            if (cursor.moveToFirst()) {
                val nameIndex = cursor.getColumnIndex(OpenableColumns.DISPLAY_NAME)
                if (nameIndex >= 0) {
                    name = cursor.getString(nameIndex)
                }
            }
        }
    }
    
    if (name == null) {
        name = uri.path?.lastPathSegment
    }
    
    return name
}
```

## 获取文件大小

```kotlin
fun getFileSize(context: Context, uri: Uri): Long {
    var size: Long = 0
    
    if (uri.scheme == "content") {
        context.contentResolver.query(uri, null, null, null, null)?.use { cursor ->
            if (cursor.moveToFirst()) {
                val sizeIndex = cursor.getColumnIndex(OpenableColumns.SIZE)
                if (sizeIndex >= 0) {
                    size = cursor.getLong(sizeIndex)
                }
            }
        }
    }
    
    return size
}
```

## 获取MIME类型

```kotlin
fun getMimeType(context: Context, uri: Uri): String? {
    return context.contentResolver.getType(uri)
}
```

---

## 洛芙的小小日记本

> 从URI能获取文件名、大小、类型！好方便呀～🌸
