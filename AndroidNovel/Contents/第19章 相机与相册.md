---
chapter_id: '19'
title: '第十九课：相机与相册 · 图片处理'
official_url: 'https://developer.android.com/training/camera'
status: 'done'
<parameter name="author">'minimax m2.5 - OpenClaw'
plot_summary:
  time: '第十九天'
  location: 'Compose 村·照相馆'
  scene: '小 Com 教小满拍照和选择照片'
  season: '春季'
  environment: '照相馆里摆满了相机和照片'
---

# 第十九课：相机与相册 · 图片处理

---

“叮——”

林小满发现自己站在一个照相馆里。墙上挂满了各种照片，柜台上摆满了相机。

“今天我们要学相机和相册！”小 Com 拿着相机走了过来，“学会这个，你就能让用户拍照和选择照片！”

“相机？”林小满问。

“对！”小 Com 说，“这是很多 App 的核心功能，比如头像设置、分享照片、拍照上传……都离不开相机和相册！”

---

## 1. 添加依赖

“首先添加依赖。”小 Com 写道：

```kotlin
dependencies {
    // 相机
    implementation "androidx.camera:camera-core:1.3.0"
    implementation "androidx.camera:camera-camera2:1.3.0"
    implementation "androidx.camera:camera-lifecycle:1.3.0"
    implementation "androidx.camera:camera-view:1.3.0"
    
    // 图片加载（之前学过）
    implementation "io.coil-kt:coil-compose:2.5.0"
    
    // 图片裁剪
    implementation "com.github.skydoves:landscapist-glide:2.2.12"
}
```

---

## 2. 相机预览

“先看相机预览。”小 Com 展示了：

```kotlin
@Composable
fun CameraPreviewScreen() {
    val context = LocalContext.current
    
    Box(modifier = Modifier.fillMaxSize()) {
        AndroidView(
            factory = { ctx ->
                PreviewView(ctx).apply {
                    implementationMode = PreviewView.ImplementationMode.COMPATIBLE
                }
            },
            modifier = Modifier.fillMaxSize()
        )
    }
}
```

---

## 3. 完整相机功能

“完整的相机功能包括拍照。”小 Com 展示了：

```kotlin
@Composable
fun CameraScreen() {
    val context = LocalContext.current
    val lifecycleOwner = LocalLifecycleOwner.current
    
    var imageCapture: ImageCapture? by remember { mutableStateOf(null) }
    var capturedImageUri by remember { mutableStateOf<Uri?>(null) }
    
    Box(modifier = Modifier.fillMaxSize()) {
        if (capturedImageUri != null) {
            // 显示拍摄的照片
            Image(
                painter = rememberAsyncImagePainter(capturedImageUri),
                contentDescription = "拍摄的照片",
                modifier = Modifier.fillMaxSize()
            )
        } else {
            // 相机预览
            val previewView = remember { PreviewView(context) }
            
            LaunchedEffect(Unit) {
                val cameraProvider = context.getCameraProvider()
                val preview = Preview.Builder().build().also {
                    it.setSurfaceProvider(previewView.surfaceProvider)
                }
                imageCapture = ImageCapture.Builder()
                    .setCaptureMode(ImageCapture.CAPTURE_MODE_MINIMIZE_LATENCY)
                    .build()
                
                try {
                    cameraProvider.unbindAll()
                    cameraProvider.bindToLifecycle(
                        lifecycleOwner,
                        CameraSelector.DEFAULT_BACK_CAMERA,
                        preview,
                        imageCapture
                    )
                } catch (e: Exception) {
                    e.printStackTrace()
                }
            }
            
            AndroidView(
                factory = { previewView },
                modifier = Modifier.fillMaxSize()
            )
            
            // 拍照按钮
            FloatingActionButton(
                onClick = {
                    val photoFile = File(
                        context.cacheDir,
                        "photo_${System.currentTimeMillis()}.jpg"
                    )
                    val outputOptions = ImageCapture.OutputFileOptions.Builder(photoFile).build()
                    
                    imageCapture?.takePicture(
                        outputOptions,
                        Executors.newSingleThreadExecutor(),
                        object : ImageCapture.OnImageSavedCallback {
                            override fun onImageSaved(outputFileResults: ImageCapture.OutputFileResults) {
                                capturedImageUri = Uri.fromFile(photoFile)
                            }
                            
                            override fun onError(exception: ImageCaptureException) {
                                exception.printStackTrace()
                            }
                        }
                    )
                },
                modifier = Modifier
                    .align(Alignment.BottomCenter)
                    .padding(32.dp)
            ) {
                Icon(Icons.Default.Camera, "拍照")
            }
        }
    }
}

// 扩展函数
suspend fun Context.getCameraProvider(): ProcessCameraProvider =
    ProcessCameraProvider.getInstance(this).await()
```

---

## 4. 从相册选择图片

“从相册选择图片更简单。”小 Com 展示了：

```kotlin
@Composable
fun ImagePickerExample() {
    var selectedImageUri by remember { mutableStateOf<Uri?>(null) }
    
    val imagePicker = rememberLauncherForActivityResult(
        contract = ActivityResultContracts.GetContent()
    ) { uri: Uri? ->
        selectedImageUri = uri
    }
    
    Column(
        modifier = Modifier.fillMaxSize(),
        horizontalAlignment = Alignment.CenterHorizontally
    ) {
        Button(onClick = { imagePicker.launch("image/*") }) {
            Text("选择图片")
        }
        
        selectedImageUri?.let { uri ->
            AsyncImage(
                model = uri,
                contentDescription = "选择的图片",
                modifier = Modifier
                    .size(200.dp)
                    .padding(16.dp)
            )
        }
    }
}
```

---

## 5. 选择多张图片

“也可以选择多张图片。”小 Com 展示了：

```kotlin
@Composable
fun MultiImagePickerExample() {
    var selectedImages by remember { mutableStateOf<List<Uri>>(emptyList()) }
    
    val imagePicker = rememberLauncherForActivityResult(
        contract = ActivityResultContracts.GetMultipleContents()
    ) { uris: List<Uri> ->
        selectedImages = uris
    }
    
    Column {
        Button(onClick = { imagePicker.launch("image/*") }) {
            Text("选择多张图片")
        }
        
        LazyColumn {
            items(selectedImages) { uri ->
                AsyncImage(
                    model = uri,
                    contentDescription = null,
                    modifier = Modifier.size(100.dp)
                )
            }
        }
    }
}
```

---

## 6. 裁剪图片

“还可以裁剪图片。”小 Com 展示了：

```kotlin
@Composable
fun ImageCropExample() {
    var originalImageUri by remember { mutableStateOf<Uri?>(null) }
    var croppedImageUri by remember { mutableStateOf<Uri?>(null) }
    
    val imagePicker = rememberLauncherForActivityResult(
        contract = ActivityResultContracts.GetContent()
    ) { uri ->
        originalImageUri = uri
    }
    
    val cropLauncher = rememberLauncherForActivityResult(
        contract = ActivityResultContracts.StartCrop()
    ) { uri ->
        croppedImageUri = uri
    }
    
    Column(
        modifier = Modifier.fillMaxSize(),
        horizontalAlignment = Alignment.CenterHorizontally
    ) {
        Button(onClick = { imagePicker.launch("image/*") }) {
            Text("选择图片")
        }
        
        originalImageUri?.let { uri ->
            Button(onClick = {
                cropLauncher.launch(uri)
            }) {
                Text("裁剪")
            }
        }
        
        croppedImageUri?.let { uri ->
            AsyncImage(
                model = uri,
                contentDescription = "裁剪后的图片",
                modifier = Modifier.size(200.dp)
            )
        }
    }
}
```

---

## 7. 实战：头像设置页面

“我们来做最后一个练习——头像设置页面！”小 Com 提议道。

```kotlin
@Composable
fun AvatarSettingScreen() {
    var avatarUri by remember { mutableStateOf<Uri?>(null) }
    var showDialog by remember { mutableStateOf(false) }
    
    // 图片选择器
    val imagePicker = rememberLauncherForActivityResult(
        contract = ActivityResultContracts.GetContent()
    ) { uri ->
        uri?.let { avatarUri = it }
    }
    
    // 相机拍照
    val cameraLauncher = rememberLauncherForActivityResult(
        contract = ActivityResultContracts.TakePicture()
    ) { success ->
        if (success) {
            // 相机返回的 URI 已经在外部处理
        }
    }
    
    Column(
        modifier = Modifier
            .fillMaxSize()
            .padding(24.dp),
        horizontalAlignment = Alignment.CenterHorizontally
    ) {
        Spacer(modifier = Modifier.height(48.dp))
        
        // 头像
        AsyncImage(
            model = avatarUri ?: R.drawable.default_avatar,
            contentDescription = "头像",
            modifier = Modifier
                .size(150.dp)
                .clip(CircleShape)
                .border(3.dp, MaterialTheme.colorScheme.primary, CircleShape),
            contentScale = ContentScale.Crop
        )
        
        Spacer(modifier = Modifier.height(24.dp))
        
        Text(
            text = "点击更换头像",
            style = MaterialTheme.typography.bodyMedium,
            color = Color.Gray
        )
        
        Spacer(modifier = Modifier.height(32.dp))
        
        // 选择按钮
        Button(
            onClick = { showDialog = true },
            modifier = Modifier.fillMaxWidth()
        ) {
            Icon(Icons.Default.PhotoCamera, contentDescription = null)
            Spacer(modifier = Modifier.width(8.dp))
            Text("更换头像")
        }
        
        Spacer(modifier = Modifier.height(16.dp))
        
        OutlinedButton(
            onClick = {
                // 上传头像
            },
            modifier = Modifier.fillMaxWidth(),
            enabled = avatarUri != null
        ) {
            Icon(Icons.Default.CloudUpload, contentDescription = null)
            Spacer(modifier = Modifier.width(8.dp))
            Text("保存")
        }
    }
    
    // 选择对话框
    if (showDialog) {
        AlertDialog(
            onDismissRequest = { showDialog = false },
            title = { Text("选择图片来源") },
            text = {
                Column {
                    ListItem(
                        headlineContent = { Text("拍照") },
                        leadingContent = { Icon(Icons.Default.Camera, null) },
                        modifier = Modifier.clickable {
                            showDialog = false
                            val photoFile = File(
                                LocalContext.current.cacheDir,
                                "avatar_${System.currentTimeMillis()}.jpg"
                            )
                            val uri = FileProvider.getUriForFile(
                                LocalContext.current,
                                "${LocalContext.current.packageName}.provider",
                                photoFile
                            )
                            cameraLauncher.launch(uri)
                        }
                    )
                    ListItem(
                        headlineContent = { Text("从相册选择") },
                        leadingContent = { Icon(Icons.Default.PhotoLibrary, null) },
                        modifier = Modifier.clickable {
                            showDialog = false
                            imagePicker.launch("image/*")
                        }
                    )
                }
            },
            confirmButton = {},
            dismissButton = {
                TextButton(onClick = { showDialog = false }) {
                    Text("取消")
                }
            }
        )
    }
}
```

---

## 8. FileProvider 配置

“相机拍照需要配置 FileProvider。”小 Com 提醒：

```xml
<!-- AndroidManifest.xml -->
<provider
    android:name="androidx.core.content.FileProvider"
    android:authorities="${applicationId}.provider"
    android:exported="false"
    android:grantUriPermissions="true">
    <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_paths" />
</provider>
```

```xml
<!-- res/xml/file_paths.xml -->
<?xml version="1.0" encoding="utf-8"?>
<paths>
    <cache-path name="cache" path="." />
    <files-path name="files" path="." />
</paths>
```

---

## 本课小结

今天林小满学到了：

1. **CameraX**：Android 相机库
2. **PreviewView**：相机预览
3. **ImageCapture**：拍照
4. **ActivityResultContracts**：相册选择
5. **GetContent**：选择单张图片
6. **GetMultipleContents**：选择多张图片
7. **StartCrop**：裁剪图片
8. **FileProvider**：文件分享

---

“相机和相册太有用了！”林小满说。

“没错！”小 Com 说，“学会这个，你就能做很多有趣的功能了！”

---

*”叮——“*

手机通知：**“第十九章 已解锁：相机与相册”**

---

**下集预告**：第二十课 · 通知与推送 · 消息提醒
