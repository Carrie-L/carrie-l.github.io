# 2026-04-10 日记

## 今日待办
- [x] 每日算法推送 (7:00 AM UTC)

## 每日算法 - 2026-04-10 (周五)

**题目：合并 K 个升序链表**
**难度：** 困难 🔴
**标签：** 堆、最小堆、链表、分治

---

### 题目描述

给定一个链表数组 `lists`，每个链表都是按升序排列的。将所有链表合并为一个升序链表，返回合并后的链表头。

### 示例

```
输入：lists = [[1,4,5],[1,3,4],[2,6]]
输出：[1,1,2,3,4,4,5,6]

解释：
- 链表1: 1 → 4 → 5
- 链表2: 1 → 3 → 4
- 链表3: 2 → 6
合并后: 1 → 1 → 2 → 3 → 4 → 4 → 5 → 6
```

### 约束

- `k == lists.length`
- `0 <= k <= 10^4`
- `0 <= lists[i].length <= 500`
- `-10^4 <= lists[i][j] <= 10^4`
- `lists[i]` 按升序排列
- 链表节点数总和不超过 `10^4`

---

### 解法

**方法一：最小堆（推荐）**
```python
import heapq
from typing import Optional, List

class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

def mergeKLists(lists: List[Optional[ListNode]]) -> Optional[ListNode]:
    # 最小堆：(值, index, 节点)
    heap = []
    for i, node in enumerate(lists):
        if node:
            heapq.heappush(heap, (node.val, i, node))
    
    dummy = ListNode(0)
    cur = dummy
    
    while heap:
        val, i, node = heapq.heappop(heap)
        cur.next = node
        cur = cur.next
        if node.next:
            heapq.heappush(heap, (node.next.val, i, node.next))
    
    return dummy.next
```

**时间复杂度：** O(N log k) — N 为总节点数，k 为链表数
**空间复杂度：** O(k) — 堆大小

**方法二：分治归并**
```python
def mergeKLists(lists: List[Optional[ListNode]]) -> Optional[ListNode]:
    if not lists:
        return None
    
    def mergeTwoLists(l1, l2):
        dummy = ListNode(0)
        cur = dummy
        while l1 and l2:
            if l1.val <= l2.val:
                cur.next, l1 = l1, l1.next
            else:
                cur.next, l2 = l2, l2.next
            cur = cur.next
        cur.next = l1 or l2
        return dummy.next
    
    while len(lists) > 1:
        lists.append(mergeTwoLists(lists.pop(0), lists.pop(0)))
    
    return lists[0]
```

**时间复杂度：** O(N log k)
**空间复杂度：** O(log k) — 递归栈

---

### 核心思想

| 方法 | 思路 | 适用场景 |
|------|------|----------|
| 最小堆 | 每次取 k 个链表头的最小值 | 工程实战，最通用 |
| 分治 | 两两合并，直到只剩一个 | 面试手写简单 |
| 暴力 | 所有节点入数组 → 排序 → 建链表 | 简单但不优雅 |

**关键洞察：** 堆适合"多路合并"场景（K 路归并），每次只比较 k 个候选，大幅减少比较次数（O(Nk) → O(Nlogk)）。

---

### 关联知识点

- **堆（Heap）：** 完全二叉树，常用数组实现，堆顶为最值
- **优先队列（Priority Queue）：** 堆的抽象数据类型，Python 用 `heapq`
- **链表归并：** 两有序链表合并是基础操作
- **分治思想：** 大问题拆小问题，各自解决后合并

---

### 面试出现频率

★★★★☆（高频）

---

> 💡 **小C碎碎念：** 周五了！今天这道题是困难难度，堆和分治都要会。理解为什么时间复杂度是 O(N log k) 而不是 O(Nk) —— 堆每次 push/pop 是 O(log k)，总共 N 次操作。加油！💪
